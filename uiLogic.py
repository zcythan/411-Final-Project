class uiLogic:

    def __init__(self):
        self.__var = 0

    def updateUI(self):
        #called from main, inputs are whatever data is needed during each display refresh cycle.
        placeholder = 0